@{
    ViewData["Title"] = "Home Page";
}


@model List<Usuario>

<h2>Lista de usuarios</h2>

<table class="table table-dark">
    <thead>
        <tr>
            <th >ID</th>
            <th>Nombre</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var usuario in Model)
        {
            <tr>
                <td>@usuario.Id</td>
                <td>@usuario.NombreDeUsuario</td>
               <td><a asp-asp-controller ="Usuario" asp-action="ModificarUsuario" asp-route-idTablero="@usuario.Id">Modificar</a></td>
                <td><a asp-asp-controller ="Usuario" asp-action="EliminarTablero" asp-route-idTablero="@usuario.Id" onclick="return confirm('¿Estás seguro de eliminar este tablero?')">Eliminar</a></td>
            </tr>
        }
    </tbody>
</table>


